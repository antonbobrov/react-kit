import useDeepCompareEffect, {
  useDeepCompareMemoize,
  useDeepCompareEffectNoCheck,
} from 'use-deep-compare-effect';

// This hook is reexported from `use-deep-compare-effect` for convenience.

export {
  useDeepCompareEffect,
  useDeepCompareMemoize,
  useDeepCompareEffectNoCheck,
};
